apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# PPPoE session lifecycle test: PADI/PADO/PADR/PADS/LCP/PAP/IPCP
resources:
  - deployment.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: bng-edge
      app.kubernetes.io/component: pppoe-test
