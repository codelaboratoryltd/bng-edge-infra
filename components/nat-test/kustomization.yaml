apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# NAT44/CGNAT test: port blocks, hairpinning, logging
resources:
  - deployment.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: bng-edge
      app.kubernetes.io/component: nat-test
