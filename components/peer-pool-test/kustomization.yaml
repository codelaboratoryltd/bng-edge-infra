apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Peer pool test: distributed IP allocation without Nexus
resources:
  - deployment.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: bng-edge
      app.kubernetes.io/component: peer-pool-test
