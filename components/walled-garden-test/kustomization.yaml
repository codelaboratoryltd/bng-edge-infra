apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Walled garden test: unknown -> walled garden -> production lifecycle
resources:
  - deployment.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: bng-edge
      app.kubernetes.io/component: walled-garden-test
