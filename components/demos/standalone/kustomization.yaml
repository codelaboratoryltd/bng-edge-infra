apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Demo A: Standalone BNG (no Nexus)
# BNG runs in demo mode with simulated subscribers

namespace: demo-standalone

resources:
  - namespace.yaml
  - ../../base/bng

namePrefix: standalone-

labels:
  - pairs:
      demo: standalone
