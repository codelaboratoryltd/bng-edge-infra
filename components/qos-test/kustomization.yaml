apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# QoS test: per-subscriber TC rate limiting with eBPF
resources:
  - deployment.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: bng-edge
      app.kubernetes.io/component: qos-test
