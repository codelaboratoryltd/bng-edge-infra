apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# BGP/FRR integration test: peering, subscriber route injection, BFD
resources:
  - deployment.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: bng-edge
      app.kubernetes.io/component: bgp-test
